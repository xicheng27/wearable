import SignInClient from "./SignInClient";

export const metadata = {
  title: "Sign in – Xi's",
  description: "Sign in to Xi's to pick up where you left off.",
};

export default function SignInPage() {
  return <SignInClient />;
}
