import SingaporeClient from "./SingaporeClient";

export const metadata = {
  title: "Adaptive Clothing in Singapore – Xi's",
  description:
    "A sourced directory of adaptive clothing brands and pieces available to shoppers in Singapore — local labels, brands that ship here, and Singapore initiatives.",
};

export default function SingaporePage() {
  return <SingaporeClient />;
}
